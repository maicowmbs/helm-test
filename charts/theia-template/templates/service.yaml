apiVersion: v1
kind: Service
metadata:
  name: {{ include "theia-template.fullname" . }}
  labels:
    {{- include "theia-template.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "theia-template.selectorLabels" . | nindent 4 }}
