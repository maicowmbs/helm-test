apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "theia-template.fullname" . }}-test-connection"
  labels:
    {{- include "theia-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "theia-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
