apiVersion: v1
kind: Service
metadata:
  name: {{ include "michael.fullname" . }}
  labels:
    {{- include "michael.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "michael.selectorLabels" . | nindent 4 }}
